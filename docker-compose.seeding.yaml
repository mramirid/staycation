services:
  seeding:
    build:
      context: ./cms
      dockerfile: Dockerfile.seed
    env_file:
      - ./cms/.env.production
    volumes:
      - public_images:/app/public/images
    depends_on:
      - mongodb
